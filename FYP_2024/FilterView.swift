import SwiftUI

struct FilterView: View {
    var body: some View {
        Text("Filter View waiting for update")
        Image("cat")
            .resizable()
            .scaledToFit()
    }
}

#Preview {
    FilterView()
}
